# DogeRat Web Admin - Environment Configuration
# Copy this file to .env and update with your actual values

# ============================================
# SERVER CONFIGURATION
# ============================================
NODE_ENV=development
PORT=5000
HOST=0.0.0.0

# ============================================
# FRONTEND & CORS
# ============================================
FRONTEND_URL=http://localhost:4200
CORS_ORIGIN=http://localhost:4200

# ============================================
# DATABASE CONFIGURATION
# ============================================
# PostgreSQL (recommended)
DATABASE_URL=postgresql://dogerat:changeme@localhost:5432/dogerat
DB_TYPE=postgres

# MySQL (alternative)
# DATABASE_URL=mysql://dogerat:changeme@localhost:3306/dogerat
# DB_TYPE=mysql

# For Docker
DB_NAME=dogerat
DB_USER=dogerat
DB_PASSWORD=changeme
DB_PORT=5432

# ============================================
# AUTHENTICATION & SECURITY
# ============================================
# Generate strong secret: openssl rand -base64 64
JWT_SECRET=change-this-to-a-random-secret-key-in-production
JWT_EXPIRY=24h

# Rate limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# ============================================
# FILE UPLOAD
# ============================================
MAX_FILE_SIZE=52428800
UPLOAD_DIR=./uploads

# ============================================
# LOGGING
# ============================================
LOG_LEVEL=info
LOG_FILE=./logs/server.log

# ============================================
# SOCKET.IO
# ============================================
SOCKET_PING_INTERVAL=30000
SOCKET_PING_TIMEOUT=60000

# ============================================
# ADMIN USER (First-time Setup)
# ============================================
ADMIN_USERNAME=admin
ADMIN_EMAIL=admin@dogerat.local
ADMIN_PASSWORD=Admin@123456

# ⚠️ CHANGE THE ADMIN PASSWORD IMMEDIATELY AFTER FIRST LOGIN!

# ============================================
# NGROK CONFIGURATION (For Remote Access)
# ============================================
# Ngrok authtoken (set this in your private .env; leave empty in example)
NGROK_AUTHTOKEN=

# Fixed ngrok domain (e.g., your-subdomain.ngrok-free.dev)
NGROK_FIXED_DOMAIN=

# Ngrok region (us, eu, ap, au, sa, jp, in)
NGROK_REGION=us

# Serve legacy Angular static from server (set to true only if you build client)
# For Windows Desktop app, keep false to avoid mixing web UI logic
SERVE_CLIENT=false

# ============================================
# LEGACY SUPPORT (Optional)
# ============================================
DISABLE_TELEGRAM=true
# TELEGRAM_BOT_TOKEN=your-bot-token-here

# ============================================
# PRODUCTION DEPLOYMENT NOTES
# ============================================
# 1. Set NODE_ENV=production
# 2. Use strong JWT_SECRET (generate with: openssl rand -base64 64)
# 3. Configure specific CORS_ORIGIN (not *)
# 4. Use HTTPS in production
# 5. Enable database SSL connection
# 6. Set strong ADMIN_PASSWORD
# 7. Never commit .env to version control

# ============================================
# DOCKER COMPOSE PORTS
# ============================================
SERVER_PORT=5000
CLIENT_PORT=80
# DB_PORT is already defined above
